# ── LML-MED Docs Security ──
# Block direct directory listing
Options -Indexes

# Prevent direct URL access to sensitive files if server supports .htaccess
# (Applies only to Apache-based hosting)

# Deny access to raw JS/CSS source inspection via certain paths
<FilesMatch "\.(htaccess|htpasswd|ini|log|sh|sql|bak)$">
    Require all denied
</FilesMatch>

# Security Headers
<IfModule mod_headers.c>
    Header always set X-Frame-Options "DENY"
    Header always set X-Content-Type-Options "nosniff"
    Header always set Referrer-Policy "no-referrer"
    Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
    Header always set Cache-Control "no-store, no-cache, must-revalidate"
    Header always set Pragma "no-cache"
</IfModule>
